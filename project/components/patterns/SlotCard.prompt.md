La couleur du liseré encode l'état d'encadrement, jamais la discipline — c'est ce que le responsable cherche en premier.

```jsx
<SlotCard
  state="À couvrir" stateTone="danger"
  group="Sauvetage sportif" coach="Léa Morel"
  facts={['18:00–19:00', '8 nageurs', 'Bassin 25 m']}
/>
```

Colonne de jour : `display:flex; flex-direction:column; gap:var(--gap-list)`, largeur minimale 186 px.
